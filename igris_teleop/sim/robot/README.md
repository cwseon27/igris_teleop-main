# Simulator Robot Assets

This directory is for MuJoCo simulator runtime assets.

The canonical robot-control URDF and mesh assets live under `igris_teleop/robot_control/asset`. The mesh copy here is kept because the MuJoCo XML files currently reference simulator-local paths.
